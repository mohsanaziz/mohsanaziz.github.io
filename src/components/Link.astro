---
interface Props {
  href: string;
  label: string;
  class?: string;
}

const { href, label, class: className } = Astro.props;
---

<a class:list={['rounded-sm text-link', className]} href={href} target="_blank" rel="noreferrer" aria-label={label}>
  <slot />
</a>
